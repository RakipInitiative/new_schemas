openapi: 3.0.0
info:
  title: RAKIP model schemas
  description: RAKIP model definitions
  version: 1.0.5
servers: []
paths: {}
components:
  schemas:
    Reference:
      properties:
        isMainReference:
          type: boolean
          description: Indicates whether this specific publication serves as the reference description for the model. There has to be at least one reference where this field is set to "True".
        publicationType:
          type: string
          description: The type of publication, e.g. Report, Journal article, Book, Online database, etc.
        year:
          type: integer
          description: Temporal information on the publication date.
        pmid:
          type: string
          description: The PubMed ID related to this publication.
        doi:
          type: string
          description: The DOI related to this publication.
        authors:
          type: string
          description: Name and surname of the authors who contributed to this publication.
        title:
          type: string
          description: Title of the publication in which the model or the data has been described.
        abstract:
          type: string
          description: Abstract of the publication in which the model or the data has been described.
        journal:
          type: string
          description: Data on the details of the journal in which the model or the data has been described.
        volume:
          type: string
          description: Data on the details of the journal in which the model or the data has been described.
        issue:
          type: string
          description: Data on the details of the journal in which the model or the data has been described.
        status:
          type: string
          description: The status of this publication, e.g. Published, Submitted, etc.
        website:
          type: string
          description: A link to the publication description.
        comment:
          type: string
          description: Further comments related to the reference description.
      required:
       - isMainReference
       - title
    ModelCategory:
      properties:
        modelClass:
          type: string
          description: Type of model according to RAKIP classification.
        modelSubclass:
          type: array
          items:
            type: string
            description: Sub-cassification of the model given the Model Class.
        modelClassComment:
          type: string
          description: Comment about the model class.
        basicProcess:
          type: array
          items:
            type: string
            description: Defines the impact of the process on the hazard (so far only valid for process models and predictive models).
      required:
       - modelClass
    Parameter:
      properties:
        id:
          type: string
          description: An unambiguous ID given to each of the parameters - preferably autogenerated by a software tool and compatible with SBML ID requirements; only letters from A to Z, numbers and "_".
        classification:
          type: string
          description: General classification of the parameter (e.g. Input, Constant, Output...).
          enum:
           - CONSTANT
           - INPUT
           - OUTPUT
        name:
          type: string
          description: A name given to the parameter.
        description:
          type: string
          description: General description of the parameter.
        unit:
          type: string
          description: Unit of the parameter.
        dataType:
          type: string
          description: Information on the data format of the parameter, e.g. if it the input parameter is a file location or a date or a number. This is important for software tools interpreting the metadata and generate user interfaces for parameter input.
          enum:
           - INTEGER
           - DOUBLE
           - NUMBER
           - DATE
           - FILE
           - BOOLEAN
           - VECTOROFNUMBERS
           - VECTOROFSTRINGS
           - MATRIXOFNUMBERS
           - MATRIXOFSTRINGS
           - OBJECT
           - STRING
        source:
          type: string
          description: Information on the type of knowledge used to define the parameter value.
        subject:
          type: string
          description: Scope of the parameter, e.g. if it refers to an animal, a batch of animals, a batch of products, a carcass, a carcass skin, etc.
        distribution:
          type: string
          description: Distribution describing the parameter variabilty. If no distribution selected this means the value provided in "Parameter value" is a point estimate. In case a distribution is selected the value provided in "Parameter value" is a string that the model code can parse in order to sample from the named distribution.
        distributionUncertainty:
          type: string
          description: TODO
        value:
          type: string
          description: A default value for the parameter. This is mandatory (needs to be provided) for all parameters of type "Input".
        reference:
          type: string
          description: Information on the source, where the value of the parameter has been extracted from - if available. The format should use that used in other "Reference" metadata. Preferably DOI.
        variabilitySubject:
          type: string
          description: Information "per what" the variability is described. It can be variability between broiler in a flock,  variability between all meat packages sold in Denmark, variability between days, etc.
        minValue:
          type: string
          description: Numerical value of the minimum limit of the parameter that determines the range of applicability for which the model applies.
        maxValue:
          type: string
          description: Numerical value of the maximum limit of the parameter that determines the range of applicability for which the model applies.
        error:
          type: string
          description: Error of the parameter value.
      required:
       - id
       - classification
       - name
       - unit- dataType
    Study:
      properties:
        studyIdentifier:
          type: string
          description: A user defined identifier for the study.
        studyTitle:
          type: string
          description: A title for the Study.
        studyDescription:
          type: string
          description: A brief description of the study aims.
        studyDesignType:
          type: string
          description: The type of study design being employed.
        assayMeasurementType:
          type: string
          description: The measurement being observed in this assay.
        assayTechnologyType:
          type: string
          description: The technology being employed to observe this measurement.
        assayTechnologyPlatform:
          type: string
          description: The technology platform used.
        accreditationProcedure:
          type: string
          description: Accreditation procedure for the analytical method used.
        protocolName:
          type: string
          description: The name of the protocol, e.g.Extraction Protocol.
        protocolType:
          type: string
          description: The type of the protocol, preferably coming from an Ontology, e.g. Extraction Protocol.
        protocolDescription:
          type: string
          description: A description of the Protocol.
        protocolUri:
          type: string
          description: A URI to link out to a publication, web page, etc. describing the protocol.
        protocolVersion:
          type: string
          description: The version of the protocol used, where applicable.
        protocolParametersName:
          type: string
          description: The parameters used when executing this protocol.
        protocolComponentsName:
          type: string
          description: The components used when carrying out this protocol.
        protocolComponentsType:
          type: string
        assayName:
          type: string
          description: A name given to the assay.
        assayDescription:
          type: string
          description: General description of the assay. Corresponds to the Protocol REF in ISA.
        moisturePercentage:
          type: string
          description: Percentage of moisture in the original sample.
        fatPercentage:
          type: string
          description: Percentage of fat in the original sample.
        detectionLimit:
          type: string
          description: Limit of detection reported in the unit specified by the variable “Hazard unit”.
        quantificationLimit:
          type: string
          description: Limit of quantification reported in the unit specified by the variable “Hazard unit”.
        leftCensoredData:
          type: string
          description: percentage of measures equal to LOQ and/or LOD.
        contaminationRange:
          type: string
          description: Range of result of the analytical measure reported in the unit specified by the variable “Hazard unit” before censored data treatment".
        uncertaintyValue:
          type: string
          description: Indicate the expanded uncertainty (usually 95% confidence interval) value associated with the measurement expressed in the unit reported in the field “Hazard unit”.
      required:
       - studyIdentifier
       - assayName
    CorrespondingAuthor:
      properties:
        title:
          type: string
          description: Specifies the job title, functional position or function of the individual associated.
        familyName:
          type: string
          description: Family name or surname.
        givenName:
          type: string
          description: Given name.
        email:
          type: string
          description: The address for eletronic mail communication.
        telephone:
          type: string
          description: The canonical number string for a telephone number for telephony communication.
        streetAddress:
          type: string
          description: Physical delivery address.
        country:
          type: string
          description: Full name of the country in English.
        zipCode:
          type: string
          description: Postal code.
        region:
          type: string
          description: State or province.
        timeZone:
          type: string
          description: Time zone.
        gender:
          type: string
          description: Sex and gender identity. M (male), F (female), O (other), N (none or not applicable) and U (unknown).
        note:
          type: string
          description: Supplemental information or a comment.
        organization:
          type: string
          description: Organization information.
      required:
       - email
    AuthorCreator:
      properties:
        title:
          type: string
          description: Specifies the job title, functional position or function of the individual associated.
        familyName:
          type: string
          description: Family name or surname.
        givenName:
          type: string
          description: Given name.
        email:
          type: string
          description: The address for eletronic mail communication.
        telephone:
          type: string
          description: The canonical number string for a telephone number for telephony communication.
        streetAddress:
          type: string
          description: Physical delivery address.
        country:
          type: string
          description: Full name of the country in English.
        zipCode:
          type: string
          description: Postal code.
        region:
          type: string
          description: State or province.
        timeZone:
          type: string
          description: Time zone.
        gender:
          type: string
          description: Sex and gender identity. M (male), F (female), O (other), N (none or not applicable) and U (unknown).
        note:
          type: string
          description: Supplemental information or a comment.
        organization:
          type: string
          description: Organization information.
    QualityMeasures:
      properties:
        sse:
          type: number
          description: Statistical values calculated to describe the performance of the model fitting procedure
        mse:
          type: number
          description: Statistical values calculated to describe the performance of the model fitting procedure
        rmse:
          type: number
          description: Statistical values calculated to describe the performance of the model fitting procedure
        rsquared:
          type: number
          description: Statistical values calculated to describe the performance of the model fitting procedure
        aic:
          type: number
          description: Statistical values calculated to describe the performance of the model fitting procedure
        bic:
          type: number
          description: Statistical values calculated to describe the performance of the model fitting procedure
        sensitivityAnalysis:
          type: array
          items:
            type: string
            description: Description of the results of an sensitivity analysis, i.e. how independence assumptions are met or how variables will affect the output of model.
        convergence:
          type: string
          description: Comment on the convergence of the used algorithm (e.g. Monte Carlo simulation). It is possible to state experiences/problems.
    Laboratory:
      properties:
        accreditation:
          type: array
          items:
            type: string
            description: The laboratory accreditation to ISO/IEC 17025
        name:
          type: string
          description: Laboratory code (National laboratory code if available) or Laboratory name 
        country:
          type: string
          description: Country where the laboratory is placed. (ISO 3166-1-alpha-2)
      required:
       - accreditation
    Product:
      required:
       - name
       - unit
      properties:
        name:
          type: string
          description: The product, matrix or environment (e.g food product, lab media, soil etc.) for which the model or data applies.
        description:
          type: string
          description: Detailed description of the product, matrix or environment for which the model or data applies.
        unit:
          type: string
          description: Unit of the product, matrix or environment for which the model or data applies.
        method:
          type: array
          items:
            type: string
          description: Type of production for the product/ matrix.
        packaging:
          type: array
          items:
            type: string
          description: Describe container or wrapper that holds the product/matrix. Common type of packaging; paper or plastic bags, boxes, tinplate or aluminium cans, plastic trays, plastic bottles, glass bottles or jars.
        treatment:
          type: array
          items:
            type: string
          description: Used to characterise a product/matrix based on the treatment or processes applied to the product or any indexed ingredient.
        originCountry:
          type: string
          description: Country of origin of the food/product (ISO 3166-1-alpha-2 country code).
        originArea:
          type: string
          description: Area of origin of the food/product (Nomenclature of territorial units for statistics – NUTS – coding system valid only for EEA and Switzerland).
        fisheriesArea:
          type: string
          description: Fisheries or aquaculture area specifying the origin of the sample (FAO Fisheries areas).
        productionDate:
          type: string
          format: date
          description: Date of production of food/product.
        expiryDate:
          type: string
          format: date
          description: Date of expiry of food/product.
    Hazard:
      type: object
      required:
       - name
      properties:
        type:
          type: string
          description: General classification of the hazard  for which the model or data applies.
        name:
          type: string
          description: Name of the hazard (agent, contaminant, chemical) for which the data or model applies.
        description:
          type: string
          description: Description of the hazard for which the model or data applies.
        unit:
          type: string
          description: Unit of the hazard for which the model or data applies.
        adverseEffect:
          type: string
          description: Adverse effect induced by hazard about morbidity, mortality, and etcetera.
        sourceOfContamination:
          type: string
          description: Origin of the contamination, source.
        benchmarkDose:
          type: string
          description: A dose or concentration that produces a predetermined change in response rate of an adverse effect (called the benchmark response or BMR) compared to background
        maximumResidueLimit:
          type: string
          description: International regulations and permissible maximum residue levels in food and drinking water
        noObservedAdverseAffectLevel:
          type: string      
          description: Level of exposure of an organism, found by experiment or observation, at which there is no biologically or statistically significant increase in the frequency or severity of any adverse effects in the exposed population when compared to its appropriate control
        lowestObservedAdverseAffectLevel:
          type: string
          description: Lowest concentration or amount of a substance found by experiment or observation that causes an adverse alteration of morphology, function, capacity, growth, development, or lifespan of a target organism distinguished from normal organisms of the same species under defined conditions of exposure
        acceptableOperatorsExposureLevel:
          type: string
          description: Maximum amount of active substance to which the operator may be exposed without any adverse health effects. The AOEL is expressed as milligrams of the chemical per kilogram body weight of the operator
        acuteReferenceDose:
          type: string
          description: An estimate (with uncertainty spanning perhaps an order of magnitude) of a daily oral exposure for an acute duration (24 hours or less) to the human population (including sensitive subgroups) that is likely to be without an appreciable risk of deleterious effects during a lifetime
        acceptableDailyIntake:
          type: string
          description: Measure of amount of a specific substance in food or in drinking water tahta can be ingested (orally) on a daily basis over a lifetime without an appreciable health risk
        indSum:
          type: string
          description: Define if the parameter reported is an individual residue/analyte, a summed residue definition or part of a sum a summed residue definition
        toxicologicalValue:
          type: string
    PopulationGroup:
      required:
       - name
      properties:
        name:
          type: string
          description: Name of the population for which the model or data applies
        targetPopulation:
          type: string
          description: Population of individual that we are interested in describing and making statistical inferences about.
        exposureDuration:
          type: array
          items:
            type: string
            description: Temporal information on the exposure duration.
        populationDescription:
          type: array
          items:
            type: string
            description: Description of the population for which the model applies (demographic and socio-economic characteristics for example). Background information that are needed in the data analysis phase, size of household, education level, employment status, professional category, ethnicity, etc.
        populationAge:
          type: array
          items:
            type: string
            description: Description of the range of age or group of age
        populationGender:
          type: string
          description: Description of the percentage of gender
        bmi:
          type: array
          items:
            type: string
            description: Description of the range of BMI or class of BMI or BMI mean
        specialDietGroups:
          type: array
          items:
            type: string
            description: Description of sub-population with special diets (vegetarians, diabetics, group following special ethnic diets)
        patternConsumption:
          type: array
          items:
            type: string
            description: Description of the consumption of different food items, frequency, portion size
        region:
          type: array
          items:
            type: string
            description: Spatial information (area) on which the population group of the model or data applies
        country:
          type: array
          items:
            type: string
            description: Country on which the population group of the model or data applies
        populationRiskFactor:
          type: array
          items:
            type: string
            description: Population risk factor that may influence the outcomes of the study, confounder should be included
        season:
          type: array
          items:
            type: string
            description: Distribution of surveyed people according to the season (influence consumption pattern)
    
    StudySample:
      required:
       - sampleName
       - protocolOfSampleCollection
       - samplingPlan
       - samplingWeight
       - samplingSize
      properties:
        sampleName:
          type: string
          description: An unambiguous ID given to the samples used in the assay
        protocolOfSampleCollection:
          type: string
          description: Additional protocol for sample and sample collection. Corresponds to the Protocol REF in ISA
        samplingStrategy:
          type: string
          description: Sampling strategy (ref. EUROSTAT - Typology of sampling strategy, version of July 2009)
        typeOfSamplingProgram:
          type: string
          description: Indicate the type of programm for which the samples have been collected
        samplingMethod:
          type: string
          description: Sampling method used to take the sample
        samplingPlan:
          type: string
          description: Description of data collection technique (stratified or complex sampling (several stages))
        samplingWeight:
          type: string
          description: Description of the method employed to compute sampling weight (nonresponse-adjusted weight)
        samplingSize:
          type: string
          description: Number of units, full participants, partial participants, eligibles, not eligible, unresolved (eligibility status not resolved)…
        lotSizeUnit:
          type: string
          description: Unit in which the lot size is expressed
        samplingPoint:
          type: string
          description: Point in the food chain where the sample was taken. (Doc. ESTAT/F5/ES/155 “Data dictionary of activities of the establishments”)

    DietaryAssessmentMethod:
      required:
       - collectionTool
       - numberOfNonConsecutiveOneDay
       - numberOfFoodItems
       - recordTypes
       - foodDescriptors
      properties:
        collectionTool:
          type: string
          description: Food diaries, interview, 24-hour recall interview, food propensy questionnaire, portion size measurement aids, eating outside questionnaire
        numberOfNonConsecutiveOneDay:
          type: string
          description: Number of non-consecutive one-day recorded
        softwareTool:
          type: string
          description: Name of the software used to collect the data
        numberOfFoodItems:
          type: array
          description: Number of food items
          items:
            type: string
        recordTypes:
          type: array
          description: Consumption occasion, mean of consumption, quantified and described as eaten, recipes for self-made
          items:
            type: string
        foodDescriptors:
          type: array
          description: Description using FoodEx2 facet
          items:
            type: string

    ModelEquation:
      required:
      - name
      - modelEquation
      properties:
        name:
          type: string
          description: A name given to the model equation
        modelEquationClass:
          type: string
          description: Information on that helps to categorize model equations
        reference:
          type: array
          items:
            $ref: '#/components/schemas/Reference'
            description: Information on the source, where the equation has been extracted from
        modelEquation:
          type: string
          description: The pointer to the file that holds the software code (e.g. R-script)
        modelHypothesis:
          type: array
          items:
            type: string
            description: Description of the hypothesis of the model

    Exposure:
      required:
       - type
      properties:
        treatment:
          description: Description of the mathematical method to replace left-censored data (recommandation of WHO (2013), distribution or others)
          type: array
          items:
            type: string
        contamination:
          description: Description of the range of of the level of contamination after left censored data treatment
          type: array
          items:
            type: string
        type:
          description: Description of the type of exposure
          type: string
        scenario:
          type: array
          description: Description of the different scenarii used in exposure assessment
          items:
            type: string
        uncertaintyEstimation:
          description: Analysis to estimate uncertainty
          type: string

    Simulation:
      required:
       - name
      properties:
        name:
          type: string
        description:
          type: string

    Model:
      type: object
      required:
       - modelType
      properties:
        modelType:
          type: string

    GenericModel:
      allOf:
        - $ref: '#/components/schemas/Model'
        - type: object
          properties:
            generalInformation:
              type: object
              required:
               - name
               - identifier
               - rights
               - format
               - description
              properties:
                name:
                  type: string
                  description: A name given to the model or data
                source:
                  type: string
                  description: A source from which the model/data is derived
                identifier:
                  type: string
                  description: An unambiguous ID given to the model or data. This can also be created automatically by a software tool.
                author:
                  type: array
                  items:
                    $ref: '#/components/schemas/AuthorCreator'
                  description: Person(s) who generated the model code or generated the data set originally
                correspondingCreator:
                  type: array
                  items:
                    $ref: '#/components/schemas/CorrespondingCreator'
                  description: The responsible person(s) that created this FSKX file including all metadata
                otherCreators:
                  type: array
                  items:
                    $ref: '#/components/schemas/AuthorCreator'
                  description: The person(s) that contributed to the creation of this FSKX file including all metadata
                creationDate:
                  type: string
                  format: date
                  description: Creation date/time of the FSK file
                modificationDate:
                  type: array
                  items:
                    type: string
                    format: date
                  description: Date/time of the last version of the FSK file
                rights:
                  type: string
                  description: Rights granted for usage, distribution and modification of this FSK file.
                availability:
                  type: string
                  description: Availability of data or model, i.e. if the annotated model code / data is included in this FSK file
                url:
                  type: string
                  format: uri
                  description: Web address referencing the resource location (data for example).
                format:
                  type: string
                  description: File extension of the model or data file (including version number of format if applicable).
                reference:
                  type: array
                  items:
                    $ref: '#/components/schemas/Reference'
                language:
                  type: string
                  description: Languages of the resources like reports (references publications) as well as data sources (data bases) and data (input and/or output of the model).
                software:
                  type: string
                  description: The program or software language in which the model has been implemented.
                languageWrittenIn:
                  type: string
                  description: Software language used to write the model, e.g. R or MatLab.
                modelCategory:
                  $ref: '#/components/schemas/ModelCategory'
                status:
                  type: string
                  description: The curation status of the model as defined by the platform where the model is made available.
                objective:
                  type: string
                  description: Objective of the model or data.
                description:
                  type: string
                  description: General description of the study, data or model.
                version:
                  type: string
            scope:
              type: object
              properties:
                product:
                  type: array
                  items:
                    $ref: '#/components/schemas/Product'
                hazard:
                  type: array
                  items:
                    $ref: '#/components/schemas/Hazard'
                populationGroup:
                  type: array
                  items:
                    $ref: '#/components/schemas/PopulationGroup'
                generalComment:
                  type: string
                time:
                  type: string
                region:
                  type: array
                  items:
                    type: string
                country:
                  type: array
                  items:
                    type: string
            dataBackground:
              type: object
              properties:
                study:
                  $ref: '#/components/schemas/Study'
                studySample:
                  type: array
                  items:
                    $ref: '#/components/schemas/StudySample'
                dietaryAssessmentMethod:
                  type: array
                  items:
                    $ref: '#/components/schemas/DietaryAssessmentMethod'
                laboratory:
                  type: array
                  items:
                    $ref: '#/components/schemas/Laboratory'
            modelMath:
              type: object
              properties:
                parameter:
                  type: array
                  items:
                    $ref: '#/components/schemas/Parameter'
                qualityMeasures:
                  type: array
                  items:
                    $ref: '#/components/schemas/QualityMeasures'
                modelEquation:
                  type: array
                  items:
                    $ref: '#/components/schemas/ModelEquation'
                fittingProcedure:
                  type: string
                  description: Procedure used to fit the data to the model equation 
                exposure:
                  type: array
                  items:
                    $ref: '#/components/schemas/Exposure'
                event:
                  type: array
                  description: Definition of time-dependent parameter changes
                  items:
                    type: string
                simulation:
                  type: array
                  items:
                    $ref: '#/components/schemas/Simulation'
                assumption:
                  type: string
                  description: Model assumptions

    DataModel:
      allOf:
        - $ref: '#/components/schemas/Model'
        - type: object
          properties:
            generalInformation:
              type: object
              required:
               - name
               - identifier
               - creationDate
               - rights
               - format
               - description
              properties:
                name:
                  type: string
                  description: A name given to the data
                source:
                  type: string
                  description: A source from which the data is derived
                identifier:
                  type: string
                  description: An unambiguous ID given to the data
                author:
                  type: array
                  items:
                    $ref: '#/components/schemas/AuthorCreator'
                  description: Person(s) who generated generated the data set originally
                correspondingCreator:
                  type: array
                  items:
                    $ref: '#/components/schemas/CorrespondingCreator'
                  description: The responsible person(s) that created this FSKX file including all metadata
                otherCreators:
                  type: array
                  items:
                    $ref: '#/components/schemas/AuthorCreator'
                  description: The person(s) that contributed to the creation of this FSKX file including all metadata
                creationDate:
                  type: string
                  format: date
                  description: Creation date/time of the FSK file
                modificationDate:
                  type: array
                  items:
                    type: string
                    format: date
                  description: Date/time of the last version of the FSK file
                rights:
                  type: string
                  description: Rights granted for usage, distribution and modification of this FSK file
                availability:
                  type: string
                  description: Availability of data
                url:
                  type: string
                  description: Web address referencing the resource location
                format:
                  type: string
                  description: Form of data (file extension)
                references:
                  type: array
                  items:
                    $ref: '#/components/schemas/Reference'
                language:
                  type: array
                  items:
                    type: string
                  description: Languages of the resources like reports (references publications) as well as data sources (data bases) and data (input and/or output of the model)
                objective:
                  type: string
                  description: Objective of the data
                description:
                  type: string
                  description: General description of the data
                version:
                  type: string
            scope:
              type: object
              properties:
                product:
                  type: array
                  items:
                    type: object
                    required:
                     - name
                     - unit
                    properties:
                      name:
                        type: string
                        description: The product-matrix (animal, food product, matrix, lab media, etc.) for which the data applies
                      description:
                        type: string
                        description: Description of the product-matrix (animal, food product, matrix, lab media, etc.) for which the data applies
                      unit:
                        type: string
                        description: Units of the product-matrix for which the data applies
                      method:
                        type: array
                        items:
                          type: string
                        description: Type of production for the product/ matrix
                      packaging:
                        type: array
                        items:
                          type: string
                        description: Describe container or wrapper that holds the product/matrix. Common type of packaging; paper or plastic bags, boxes, tinplate or aluminium cans, plastic trays, plastic bottles, glass bottles or jars
                      treatment:
                        type: array
                        items:
                          type: string
                        description: Used to characterise a product/matrix based on the treatment or processes applied to the product or any indexed ingredient
                      originCountry:
                        type: array
                        items:
                          type: string
                        description: Country of origin of the food/product (ISO 3166-1-alpha-2 country code).
                      originArea:
                        type: array
                        items:
                          type: string
                        description: Area of origin of the food/product (Nomenclature of territorial units for statistics – NUTS – coding system valid only for EEA and Switzerland)
                      fisheriesArea:
                        type: string
                        description: Fisheries or aquaculture area specifying the origin of the sample (FAO Fisheries areas).
                      productionDate:
                        type: string
                        format: date
                        description: Date of production of food/product
                      expiryDate:
                        type: string
                        format: date
                        description: Date of expiry of food/product
                hazard:
                  required:
                   - name
                  properties:
                    type:
                      type: string
                      description: General classification of the hazard for which the data applies
                    name:
                      type: string
                      description: Name of the hazard for which the data applies
                    description:
                      type: string
                      description: Description of the hazard for which the data applies
                    unit:
                      type: string
                      description: Unit of the hazard for which the model or data applies
                    adverseEffect:
                      type: array
                      items:
                        type: string
                      description: Adverse effect induced by hazard about morbidity, mortality, and etcetera
                    sourceOfContamination:
                      type: string
                      description: Origin of the contamination, source
                    benchmarkDose:
                      type: array
                      items:
                        type: string
                      description: A dose or concentration that produces a predetermined change in response rate of an adverse effect (called the benchmark response or BMR) compared to background.
                    maximumResidueLimit:
                      type: string
                      description: International regulations and permissible maximum residue levels in food and drinking water
                    noObservedAdverseAffectLevel:
                      type: string
                      description: Level of exposure of an organism, found by experiment or observation, at which there is no biologically or statistically significant increase in the frequency or severity of any adverse effects in the exposed population when compared to its appropriate control
                    lowestObservedAdverseAffectLevel:
                      type: array
                      items:
                        type: string
                        description: Lowest concentration or amount of a substance found by experiment or observation that causes an adverse alteration of morphology, function, capacity, growth, development, or lifespan of a target organism distinguished from normal organisms of the same species under defined conditions of exposure.
                    acceptableOperatorsExposureLevel:
                      type: string
                      description: Maximum amount of active substance to which the operator may be exposed without any adverse health effects. The AOEL is expressed as milligrams of the chemical per kilogram body weight of the operator
                    acuteReferenceDose:
                      type: string
                      description: An estimate (with uncertainty spanning perhaps an order of magnitude) of a daily oral exposure for an acute duration (24 hours or less) to the human population (including sensitive subgroups) that is likely to be without an appreciable risk of deleterious effects during a lifetime
                    acceptableDailyIntake:
                      type: string
                      description: Measure of amount of a specific substance in food or in drinking water tahta can be ingested (orally) on a daily basis over a lifetime without an appreciable health risk
                    indSum:
                      type: string
                      description: Define if the parameter reported is an individual residue/analyte, a summed residue definition or part of a sum a summed residue definition

                populationGroup:
                  type: array
                  items:
                    type: object
                    required:
                     - name
                    properties:
                      name:
                        type: string
                        description: Name of the population for which the data applies
                      targetPopulation:
                        type: string
                        description: Population of individual that we are interested in describing and making statistical inferences about
                      populationSpan:
                        type: array
                        items:
                          type: string
                          description: Temporal information on the exposure duration
                      populationDescription:
                        type: array
                        items:
                          type: string
                          description: Description of the population for which the model applies (demographic and socio-economic characteristics for example). Background information that are needed in the data analysis phase; size of household, education level, employment status, professional category, ethnicity, etc.
                      populationAge:
                        type: array
                        items:
                          type: string
                          description: Description of the range of age or group of age
                      populationGender:
                        type: string
                        description: Description of the percentage of gender
                      bmi:
                        type: array
                        items:
                          type: string
                          description: Description of the range of BMI or class of BMI or BMI mean
                      specialDietGroups:
                        type: array
                        items:
                          type: string
                          description: Description of sub-population with special diets (vegetarians, diabetics, group following special ethnic diets)
                      patternConsumption:
                        type: array
                        items:
                          type: string
                          description: Description of the consumption of different food items, frequency, portion size
                      region:
                        type: array
                        items:
                          type: string
                          description: Spatial information (area) on which the population group of the data applies
                      country:
                        type: array
                        items:
                          type: string
                          description: Country on which the population group of the data applies
                      populationRiskFactor:
                        type: array
                        items:
                          type: string
                          description: Population risk factor that may influence the outcomes of the study, confounder should be included
                      season:
                        type: array
                        items:
                          type: string
                          description: Distribution of surveyed people according to the season (influence consumption pattern)

                generalComment:
                  type: string
                  description: General comments on the scope of the data
                time:
                  type: string
                  description: Temporal information on which the data applies / An interval of time that is named or defined by its start and end dates (period of study)
                region:
                  type: array
                  items:
                    type: string
                  description: Spatial information (area) on which the data applies
                country:
                  type: array
                  items:
                    type: string
                  description: Country on which the data applies
            dataBackground:
              type: object
              properties:
                study:
                  $ref: '#/components/schemas/Study'
                studySample:
                  type: array
                  items:
                    $ref: '#/components/schemas/StudySample'
                dietaryAssessmentMethod:
                  type: array
                  items:
                    $ref: '#/components/schemas/DietaryAssessmentMethod'
                laboratory:
                  type: array
                  items:
                    $ref: '#/components/schemas/Laboratory'
            modelMath:
              type: object
              properties:
                parameter:
                  type: array
                  items:
                    $ref: '#/components/schemas/Parameter'
                simulation:
                  type: array
                  items:
                    $ref: '#/components/schemas/Simulation'
                assumption:
                  type: string
                  description: Model assumptions
    
    PredictiveModel:
      allOf:
        - $ref: '#/components/schemas/Model'
        - type: object
          properties:
            generalInformation:
              properties:
                name:
                  type: string
                  description: A name given to the model
                source:
                  type: string
                  description: A source from which the model/data is derived
                author:
                  type: array
                  items:
                    $ref: '#/components/schemas/AuthorCreator'
                  description: Person(s) who generated the model code or generated the data set originally
                correspondingCreator:
                  type: array
                  items:
                    $ref: '#/components/schemas/CorrespondingCreator'
                  description: The responsible person(s) that created this FSKX file including all metadata
                otherCreators:
                  type: array
                  items:
                    $ref: '#/components/schemas/AuthorCreator'
                  description: The person(s) that contributed to the creation of this FSKX file including all metadata
                creationDate:
                  type: string
                  format: date
                  description: Creation date/time of the FSK file
                modificationDate:
                  type: array
                  items:
                    type: string
                    format: date
                  description: Date/time of the last version of the FSK file
                rights:
                  type: string
                  description: Rights granted for usage, distribution and modification of this FSK file
                availability:
                  type: string
                  description: Availability of model
                url:
                  type: string
                  description: Web adress referencing the resource location
                format:
                  type: string
                references:
                  type: array
                  items:
                    $ref: '#/components/schemas/Reference'
                language:
                  type: array
                  items:
                    type: string
                  description: Languages of the resources like reports (references publications) as well as data sources (data bases) and data (input and/or output of the model)
                software:
                  type: string
                  description: The program or software language in which the model has been implemented
                languageWrittenIn:
                  type: array
                  items:
                    type: string
                  description: Software language used to write the model, e.g. R or MatLab
                modelCategory:
                  $ref: '#/components/schemas/ModelCategory'
                status:
                  type: string
                  description: The curation status of the model as defined by the platform where the model is made available.
                objective:
                  type: string
                  description: Objective of the model
                description:
                  type: string
                  description: General description of the model
                version:
                  type: string
            scope:
              type: object
              properties:
                # Product with optional unit
                product:
                  type: array
                  items:
                    type: object
                    required:
                    - name
                    properties:
                      name:
                        type: string
                        description: The product, matrix or environment (e.g food product, lab media, soil etc.) for which the model or data applies.
                      description:
                        type: string
                        description: Detailed description of the product, matrix or environment for which the model or data applies.
                      unit:
                        type: string
                        description: Unit of the product, matrix or environment for which the model or data applies.
                      method:
                        type: array
                        items:
                          type: string
                        description: Type of production for the product/ matrix.
                      packaging:
                        type: array
                        items:
                          type: string
                        description: Describe container or wrapper that holds the product/matrix. Common type of packaging; paper or plastic bags, boxes, tinplate or aluminium cans, plastic trays, plastic bottles, glass bottles or jars.
                      treatment:
                        type: array
                        items:
                          type: string
                        description: Used to characterise a product/matrix based on the treatment or processes applied to the product or any indexed ingredient.
                      originCountry:
                        type: string
                        description: Country of origin of the food/product (ISO 3166-1-alpha-2 country code).
                      originArea:
                        type: string
                        description: Area of origin of the food/product (Nomenclature of territorial units for statistics – NUTS – coding system valid only for EEA and Switzerland).
                      fisheriesArea:
                        type: string
                        description: Fisheries or aquaculture area specifying the origin of the sample (FAO Fisheries areas).
                      productionDate:
                        type: string
                        format: date
                        description: Date of production of food/product.
                      expiryDate:
                        type: string
                        format: date
                        description: Date of expiry of food/product.
                hazard:
                  type: array
                  items:
                    $ref: '#/components/schemas/Hazard'
                generalComment:
                  type: string
                  description: General comments on the scope of the model
                time:
                  type: string
                  description: Temporal information on which the model applies / An interval of time that is named or defined by its start and end dates (period of study)
                region:
                  type: array
                  items:
                    type: string
                  description: Spatial information (area) on which the model applies
                country:
                  type: array
                  items:
                    type: string
                  description: Country on which the model applies
            dataBackground:
              type: object
              properties:
                study:
                  $ref: '#/components/schemas/Study'
                studySample:
                  type: array
                  items:
                    $ref: '#/components/schemas/StudySample'
                laboratory:
                  type: array
                  items:
                    $ref: '#/components/schemas/Laboratory'
            modelMath:
              type: object
              properties:
                parameter:
                  type: array
                  items:
                    $ref: '#/components/schemas/Parameter'
                qualityMeasures:
                  type: array
                  items:
                    $ref: '#/components/schemas/QualityMeasures'
                modelEquation:
                  type: array
                  items:
                    $ref: '#/components/schemas/ModelEquation'
                fittingProcedure:
                  type: string
                  description: Procedure used to fit the data to the model equation
                event:
                  type: array
                  description: Definition of time-dependent parameter changes
                  items:
                    type: string
                simulation:
                  type: array
                  items:
                    $ref: '#/components/schemas/Simulation'
                assumption:
                  type: string
                  description: Model assumptions

    OtherEmpiricalModel:
      allOf:
        - $ref: '#/components/schemas/Model'
        - type: object
          properties:
            generalInformation:
              type: object
              required:
               - name
               - identifier
               - rights
               - languageWrittenIn
              properties:
                name:
                  type: string
                  description: A name given to the model
                source:
                  type: string
                  description: A related resource from which the described is derived
                identifier:
                  type: string
                  description: An unambiguous ID given to the model
                author:
                  type: array
                  items:
                    $ref: '#/components/schemas/AuthorCreator'
                  description: Person(s) who generated the model code or generated the data set originally
                correspondingCreator:
                  type: array
                  items:
                    $ref: '#/components/schemas/CorrespondingCreator'
                  description: The responsible person(s) that created this FSKX file including all metadata
                otherCreators:
                  type: array
                  items:
                    $ref: '#/components/schemas/AuthorCreator'
                  description: The person(s) that contributed to the creation of this FSKX file including all metadata
                creationDate:
                  type: string
                  format: date
                  description: Temporal information on the model creation date
                modificationDate:
                  type: array
                  items:
                    type: string
                    format: date
                    description: Temporal information on the last modification of the model
                rights:
                  type: string
                availability:
                  type: string
                url:
                  type: string
                format:
                  type: string
                reference:
                  type: array
                  items:
                    $ref: '#/components/schemas/Reference'
                language:
                  type: array
                  items:
                    type: string
                software:
                  type: string
                languageWrittenIn:
                  type: array
                  items:
                    type: string
                modelCategory:
                  $ref: '#/components/schemas/ModelCategory'
                status:
                  type: string
                objective:
                  type: string
                description:
                  type: string
            scope:
              type: object
              properties:
                product:
                  type: array
                  items:
                    $ref: '#/components/schemas/Product'
                hazard:
                  type: array
                  items:
                    $ref: '#/components/schemas/Hazard'
                populationGroup:
                  type: array
                  items:
                    $ref: '#/components/schemas/PopulationGroup'
                generalComment:
                  type: string
                  description: General comments on the scope of the model
                time:
                  type: string
                  description: Temporal information on which the model applies / An interval of time that is named or defined by its start and end dates (period of study)
                region:
                  type: array
                  items:
                    type: string
                  description: Spatial information (area) on which the model applies
                country:
                  type: array
                  items:
                    type: string
                  description: Country on which the model applies
            dataBackground:
              type: object
              properties:
                study:
                  $ref: '#/components/schemas/Study'
                studySample:
                  type: array
                  items:
                    $ref: '#/components/schemas/StudySample'
                laboratory:
                  type: array
                  items:
                    $ref: '#/components/schemas/Laboratory'
            modelMath:
              type: object
              properties:
                parameter:
                  type: array
                  items:
                    $ref: '#/components/schemas/Parameter'
                qualityMeasures:
                  type: array
                  items:
                    $ref: '#/components/schemas/QualityMeasures'
                modelEquation:
                  type: array
                  items:
                    $ref: '#/components/schemas/ModelEquation'
                fittingProcedure:
                  type: string
                event:
                  type: array
                  items:
                    type: string
                simulation:
                  type: array
                  items:
                    $ref: '#/components/schemas/Simulation'
                assumption:
                  type: string
                  description: Model assumptions
  
    DoseResponseModel:
      allOf:
        - $ref: '#/components/schemas/Model'
        - type: object
          properties:
            generalInformation:
              type: object
              required:
               - name
               - identifier
               - rights
               - reference
               - languageWrittenIn
               - description
              properties:
                name:
                  type: string
                  description: A name given to the model
                source:
                  type: string
                  description: A related resource from which the described is derived
                identifier:
                  type: string
                  description: An unambiguous ID given to the model
                author:
                  type: array
                  items:
                    $ref: '#/components/schemas/AuthorCreator'
                  description: Person(s) who generated the model code or generated the data set originally
                correspondingCreator:
                  type: array
                  items:
                    $ref: '#/components/schemas/CorrespondingCreator'
                  description: The responsible person(s) that created this FSKX file including all metadata
                otherCreators:
                  type: array
                  items:
                    $ref: '#/components/schemas/AuthorCreator'
                  description: The person(s) that contributed to the creation of this FSKX file including all metadata
                creationDate:
                  type: string
                  format: date
                  description: Temporal information on the model creation date
                modificationDate:
                  type: array
                  items:
                    type: string
                    format: date
                    description: Temporal information on the last modification of the model
                rights:
                  type: string
                  description: Rights granted for usage, distribution and modification of this FSK file
                availability:
                  type: string
                  description: Availability of model
                url:
                  type: string
                  description: Web address referencing the resource location
                format:
                  type: string
                  description: Form of model (file extension)
                reference:
                  type: array
                  items:
                    $ref: '#/components/schemas/Reference'
                  # TODO: description
                language:
                  type: array
                  items:
                    type: string
                  description: Languages of the resources like reports (reference publications) as well as data sources (data bases) and data (input and/or output of the model)
                software:
                  type: string
                  # TODO: description
                  description: The program or software language in which the model has been implemented
                languageWrittenIn:
                  type: array
                  items:
                    type: string
                  description: Software language used to write the model, e.g. R or Matlab
                modelCategory:
                  $ref: '#/components/schemas/ModelCategory'
                  # TODO: description
                status:
                  type: string
                  description: The curation status of the model as defined by the platform where the model is made available
                objective:
                  type: string
                  description: Objective of the model
                description:
                  type: string
                  description: General description of the model
                version:
                  type: string
            scope:
              type: object
              properties:
                product:
                  type: array
                  items:
                    $ref: '#/components/schemas/Product'
                hazard:
                  type: array
                  items:
                    $ref: '#/components/schemas/Hazard'
                populationGroup:
                  type: array
                  items:
                    $ref: '#/components/schemas/PopulationGroup'
                generalComment:
                  type: string
                  description: General comments on the scope of the model
                time:
                  type: string
                  description: Temporal information on which the model applies / An interval of time that is named or defined by its start and end dates (period of study)
                region:
                  type: array
                  items:
                    type: string
                  description: Spatial information (area) on which the model applies
                country:
                  type: array
                  items:
                    type: string
                  description: Country on which the model applies
            dataBackground:
              type: object
              properties:
                study:
                  $ref: '#/components/schemas/Study'
                studySample:
                  type: array
                  items:
                    $ref: '#/components/schemas/StudySample'
                laboratory:
                  type: array
                  items:
                    $ref: '#/components/schemas/Laboratory'
            modelMath:
              type: object
              properties:
                parameter:
                  type: array
                  items:
                    $ref: '#/components/schemas/Parameter'
                qualityMeasures:
                  type: array
                  items:
                    $ref: '#/components/schemas/QualityMeasures'
                modelEquation:
                  type: array
                  items:
                    $ref: '#/components/schemas/ModelEquation'
                fittingProcedure:
                  type: string
                event:
                  type: array
                  items:
                    type: string
                simulation:
                  type: array
                  items:
                    $ref: '#/components/schemas/Simulation'
                assumption:
                  type: string
                  description: Model assumptions
    
    ExposureModel:
      allOf:
        - $ref: '#/components/schemas/Model'
        - type: object
          properties:
            generalInformation:
              type: object
              required:
               - name
               - identifier
               - rights
               - reference
               - languageWrittenIn
               - description
              properties:
                name:
                  type: string
                  description: A name given to the model
                source:
                  type: string
                  description: A related resource from which the model/data is derived
                identifier:
                  type: string
                  description: An unambiguous ID given to the model
                author:
                  type: array
                  items:
                    $ref: '#/components/schemas/AuthorCreator'
                  description: Person(s) who generated the model code or generated the data set originally
                correspondingCreator:
                  type: array
                  items:
                    $ref: '#/components/schemas/CorrespondingCreator'
                  description: The responsible person(s) that created this FSKX file including all metadata
                otherCreators:
                  type: array
                  items:
                    $ref: '#/components/schemas/AuthorCreator'
                  description: The person(s) that contributed to the creation of this FSKX file including all metadata
                creationDate:
                  type: string
                  format: date
                  description: Temporal information on the model creation date
                modificationDate:
                  type: array
                  items:
                    type: string
                    format: date
                    description: Temporal information on the last modification of the model
                rights:
                  type: string
                  description: Rights granted for usage, distribution and modification of this FSK file
                availability:
                  type: string
                  description: Availability of model
                url:
                  type: string
                  description: Web address referencing the resource location
                format:
                  type: string
                  description: Form of model (file extension)
                reference:
                  type: array
                  items:
                    $ref: '#/components/schemas/Reference'
                  # TODO: description
                language:
                  type: array
                  items:
                    type: string
                  description: Languages of the resources like reports (reference publications) as well as data sources (data bases) and data (input and/or output of the model)
                software:
                  type: string
                  # TODO: description
                  description: The program or software language in which the model has been implemented
                languageWrittenIn:
                  type: array
                  items:
                    type: string
                  description: Software language used to write the model, e.g. R or Matlab
                modelCategory:
                  $ref: '#/components/schemas/ModelCategory'
                  # TODO: description
                status:
                  type: string
                  description: The curation status of the model as defined by the platform where the model is made available
                objective:
                  type: string
                  description: Objective of the model
                description:
                  type: string
                  description: General description of the model
                version:
                  type: string
            scope:
              type: object
              properties:
                product:
                  type: array
                  items:
                    $ref: '#/components/schemas/Product'
                hazard:
                  type: array
                  items:
                    $ref: '#/components/schemas/Hazard'
                populationGroup:
                  type: array
                  items:
                    $ref: '#/components/schemas/PopulationGroup'
                generalComment:
                  type: string
                  description: General comments on the scope of the model
                time:
                  type: string
                  description: Temporal information on which the model applies / An interval of time that is named or defined by its start and end dates (period of study)
                region:
                  type: array
                  items:
                    type: string
                  description: Spatial information (area) on which the model applies
                country:
                  type: array
                  items:
                    type: string
                  description: Country on which the model applies
            dataBackground:
              type: object
              properties:
                study:
                  $ref: '#/components/schemas/Study'
                studySample:
                  type: array
                  items:
                    $ref: '#/components/schemas/StudySample'
                laboratory:
                  type: array
                  items:
                    $ref: '#/components/schemas/Laboratory'
            modelMath:
              type: object
              properties:
                parameter:
                  type: array
                  items:
                    $ref: '#/components/schemas/Parameter'
                qualityMeasures:
                  type: array
                  items:
                    $ref: '#/components/schemas/QualityMeasures'
                modelEquation:
                  type: array
                  items:
                    $ref: '#/components/schemas/ModelEquation'
                fittingProcedure:
                  type: string
                event:
                  type: array
                  items:
                    type: string
                simulation:
                  type: array
                  items:
                    $ref: '#/components/schemas/Simulation'
                assumption:
                  type: string
                  description: Model assumptions
    
    ToxicologicalModel:
      allOf:
        - $ref: '#/components/schemas/Model'
        - type: object
          properties:
            generalInformation:
              type: object
              required:
                - name
                - identifier
                - rights
                - reference
                - languageWrittenIn
                - description
              properties:
                name:
                  type: string
                  description: A name given to the model
                source:
                  type: string
                  description: A related resource from which the model/data is derived
                identifier:
                  type: string
                  description: An unambiguous ID given to the model
                author:
                  type: array
                  items:
                    $ref: '#/components/schemas/AuthorCreator'
                  description: Person(s) who generated the model code or generated the data set originally
                correspondingCreator:
                  type: array
                  items:
                    $ref: '#/components/schemas/CorrespondingCreator'
                  description: The responsible person(s) that created this FSKX file including all metadata
                otherCreators:
                  type: array
                  items:
                    $ref: '#/components/schemas/AuthorCreator'
                  description: The person(s) that contributed to the creation of this FSKX file including all metadata
                creationDate:
                  type: string
                  format: date
                  description: Temporal information on the model creation date
                modificationDate:
                  type: array
                  items:
                    type: string
                    format: date
                    description: Temporal information on the last modification of the model
                rights:
                  type: string
                  description: Rights granted for usage, distribution and modification of this FSK file
                availability:
                  type: string
                  description: Availability of model
                url:
                  type: string
                  description: Web address referencing the resource location
                format:
                  type: string
                  description: Form of model (file extension)
                reference:
                  type: array
                  items:
                    $ref: '#/components/schemas/Reference'
                  # TODO: description
                language:
                  type: array
                  items:
                    type: string
                  description: Languages of the resources like reports (reference publications) as well as data sources (data bases) and data (input and/or output of the model)
                software:
                  type: string
                  # TODO: description
                  description: The program or software language in which the model has been implemented
                languageWrittenIn:
                  type: array
                  items:
                    type: string
                  description: Software language used to write the model, e.g. R or Matlab
                modelCategory:
                  $ref: '#/components/schemas/ModelCategory'
                  # TODO: description
                status:
                  type: string
                  description: The curation status of the model as defined by the platform where the model is made available
                objective:
                  type: string
                  description: Objective of the model
                description:
                  type: string
                  description: General description of the model
                version:
                  type: string
            scope:
              type: object
              properties:
                hazard:
                  type: array
                  items:
                    $ref: '#/components/schemas/Hazard'
                populationGroup:
                  type: array
                  items:
                    $ref: '#/components/schemas/PopulationGroup'
                generalComment:
                  type: string
                  description: General comments on the scope of the model
                time:
                  type: string
                  description: Temporal information on which the model applies / An interval of time that is named or defined by its start and end dates (period of study)
                region:
                  type: array
                  items:
                    type: string
                  description: Spatial information (area) on which the model applies
                country:
                  type: array
                  items:
                    type: string
                  description: Country on which the model applies
            dataBackground:
              type: object
              properties:
                study:
                  $ref: '#/components/schemas/Study'
                studySample:
                  type: array
                  items:
                    $ref: '#/components/schemas/StudySample'
                laboratory:
                  type: array
                  items:
                    $ref: '#/components/schemas/Laboratory'
            modelMath:
              type: object
              properties:
                parameter:
                  type: array
                  items:
                    $ref: '#/components/schemas/Parameter'
                assumption:
                  type: array
                  items:
                    type: string
                qualityMeasures:
                  type: array
                  items:
                    $ref: '#/components/schemas/QualityMeasures'
                modelEquation:
                  type: array
                  items:
                    $ref: '#/components/schemas/ModelEquation'
                fittingProcedure:
                  type: string
                  description: Procedure used to fit the data to the model equation
                exposure:
                  $ref: '#/components/schemas/Exposure'
                events:
                  type: array
                  items:
                    type: string
                  description: Definition of the time-dependent parameter changes.
                simulation:
                  type: array
                  items:
                    $ref: '#/components/schemas/Simulation'

    # TODO: Process model
    # TODO: Consumption model
    # TODO: Health model
    # TODO: Risk model
